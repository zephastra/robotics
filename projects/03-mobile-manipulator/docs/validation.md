# 仿真实测记录

日期：2026-09-05。环境：本机 WSL Ubuntu、ROS 2 Lyrical、Gazebo Sim 10.4、Python 3.14.4。

## 完整任务

归档报告：[无界面完整任务](evidence/mission-headless.json)。原始运行目录：`reports/20260905-151913-799/`。

结果：`COMPLETED`，运行约 **157 秒**。仿真真实位置验收：

| 检查 | 结果 |
| --- | --- |
| 拒绝远距离隔空抓取 | 通过 |
| 取料后实际抬升 | 通过 |
| 装上托盘 | 通过 |
| 携货到达目标台 | 通过 |
| 从托盘再次抓起 | 通过 |
| 在目标台释放并保持稳定 | 通过，水平位置误差约 1.6 cm |
| 视觉返航精对位 | 通过，使用一次修正 |
| 实际回到起点 | 通过，水平位置误差约 3.5 cm |

这些是该次仿真的测量结果，不是成功率统计，也不是实物精度承诺。

最终还完成了一次同时打开 Gazebo/RViz 的全流程：[图形界面完整任务报告](evidence/mission-gui.json)。运行目录 `reports/20260905-153229-798/`，结果同样为 `COMPLETED`，运行约 **345 秒**，放置水平误差约 **1.5 cm**，视觉对位后返航水平误差约 **3.0 cm**。该次 RViz/ROS 启动日志未发现 ERROR 或 Traceback，启动器结束后相关测试进程已退出。

## 自动测试

`bash scripts/test.sh`：**28 项通过**，覆盖范围：

- SCARA 正逆运动学、工作空间、完全伸直时的浮点边界。
- 颜色目标识别、矩形标记方向、缺失/多个目标拒绝。
- 轮轴与机械臂速度上限分离。
- 相机立柱和夹爪自身激光回波过滤，保留真实近距离障碍。
- 速度联锁：未允许行驶、未收臂、心跳/关节/速度消息陈旧时输出零速度。
- 不引用历史工程路径，不通过设置物体坐标实现搬运。
- RViz 兼容地图几何与真实 OccupancyGrid 的颜色、坐标和连续栅格压缩。

依赖检查和 Python 语法编译检查通过。

## 取消任务

无界面与带 Gazebo/RViz 窗口的取消测试均通过：[无界面证据](evidence/cancel-headless.json)、[图形界面证据](evidence/cancel-gui.json)。导航运动开始后调用 `/mission/cancel`，服务确认成功，任务标记为 `CANCELLED`，底盘停止并保持静止，结果为 `PASS`。

## 联调中发现并处理的问题

- 相机内参桥接使用 Gazebo 实际发布的 `/mm/camera_info`。
- 升降轴改用带重力补偿的力控制，并用实际关节反馈等待到位。
- 关节命令周期刷新，避免只发送一次指令的脆弱性。
- 轮轴原先误用了机械臂速度上限，现已分开并增加回归测试。
- 雷达扫描过滤机器人自身支柱和处于扫描高度的夹爪回波。
- 粗导航的返航实际误差曾超出验收上限；增加基于真实相机的末端对位，不把放宽验收阈值当作修复。
- 故障报告保留最终物理状态、失败原因和总用时；视觉报告保留实际采样时间戳和画面。
- RViz 的传统 Map 显示在本机记录过纹理着色器错误；默认改为从同一 OccupancyGrid 生成几何显示，不修改系统 RViz，也不更换导航地图。

## 尚未覆盖

不包含实体硬件、任意物体抓取、桌面高度变化、复杂机械臂避障、长期成功率统计、多机器人和工业安全验证。辅助抓取/托盘固定仍使用显式的刚体约束，详情见架构和 README。
